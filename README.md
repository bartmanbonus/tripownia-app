# Tripownia v23 — clean structure

This package removes obsolete duplicate route folders. Routes /okazje, /poradniki, /parkingi, /atrakcje and /esim are handled by app/[...slug]/page.tsx via UnifiedPage.

No visual redesign in this package.
